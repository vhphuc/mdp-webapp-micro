apiVersion: v1
kind: Service
metadata:
  name: webapp-neck-label
  namespace: mdp
  labels:
    mSvc: webapp-neck-label
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    mSvc: webapp-neck-label
