apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-neck-label-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdpdev-m.monsterdigital.biz
      http:
        paths:
          - path: /neck-label
            pathType: Prefix
            backend:
              service:
                name: webapp-neck-label
                port:
                  number: 80


