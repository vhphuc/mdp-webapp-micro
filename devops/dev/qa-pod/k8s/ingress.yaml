apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-qa-pod-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdpdev-m.monsterdigital.biz
      http:
        paths:
          - path: /qa-pod
            pathType: Prefix
            backend:
              service:
                name: webapp-qa-pod
                port:
                  number: 80
