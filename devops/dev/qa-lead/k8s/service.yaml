apiVersion: v1
kind: Service
metadata:
  name: webapp-qa-lead
  namespace: mdp
  labels:
    mSvc: webapp-qa-lead
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    mSvc: webapp-qa-lead
