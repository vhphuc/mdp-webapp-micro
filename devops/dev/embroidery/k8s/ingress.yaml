apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-embroidery-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdpdev-m.monsterdigital.biz
      http:
        paths:
          - path: /embroidery
            pathType: Prefix
            backend:
              service:
                name: webapp-embroidery
                port:
                  number: 80
