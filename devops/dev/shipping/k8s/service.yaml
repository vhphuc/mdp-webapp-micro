apiVersion: v1
kind: Service
metadata:
  name: webapp-shipping
  namespace: mdp
  labels:
    mSvc: webapp-shipping
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    mSvc: webapp-shipping
