apiVersion: v1
kind: Service
metadata:
  name: webapp-washing
  namespace: mdp
  labels:
    mSvc: webapp-washing
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    mSvc: webapp-washing
