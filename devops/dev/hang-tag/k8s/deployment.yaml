apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-hang-tag
  namespace: mdp
  labels:
    mSvc: webapp-hang-tag
spec:
  selector:
    matchLabels:
      mSvc: webapp-hang-tag
  template:
    metadata:
      labels:
        mSvc: webapp-hang-tag
    spec:
      containers:
        - name: webapp-hang-tag-container
          image: monsterdigital.azurecr.io/mdp-webapp-hang-tag:dev
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 5
