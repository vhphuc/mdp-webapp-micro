apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-trim-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdptest-m.monsterdigital.biz
      http:
        paths:
          - path: /trim
            pathType: Prefix
            backend:
              service:
                name: webapp-trim
                port:
                  number: 80
