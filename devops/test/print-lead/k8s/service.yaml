apiVersion: v1
kind: Service
metadata:
  name: webapp-print-lead
  namespace: mdp
  labels:
    mSvc: webapp-print-lead
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    mSvc: webapp-print-lead
