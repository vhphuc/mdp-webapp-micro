apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-shipping-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdptest-m.monsterdigital.biz
      http:
        paths:
          - path: /shipping
            pathType: Prefix
            backend:
              service:
                name: webapp-shipping
                port:
                  number: 80
