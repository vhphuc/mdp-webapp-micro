apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-mug-transfer-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdptest-m.monsterdigital.biz
      http:
        paths:
          - path: /mug-transfer
            pathType: Prefix
            backend:
              service:
                name: webapp-mug-transfer
                port:
                  number: 80
