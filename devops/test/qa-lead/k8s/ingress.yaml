apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-qa-lead-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdptest-m.monsterdigital.biz
      http:
        paths:
          - path: /qa-lead
            pathType: Prefix
            backend:
              service:
                name: webapp-qa-lead
                port:
                  number: 80
