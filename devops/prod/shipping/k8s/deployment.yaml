apiVersion: apps/v1
kind: Deployment
metadata:
  name: webapp-shipping
  namespace: mdp
  labels:
    mSvc: webapp-shipping
spec:
  selector:
    matchLabels:
      mSvc: webapp-shipping
  template:
    metadata:
      labels:
        mSvc: webapp-shipping
    spec:
      containers:
        - name: webapp-shipping-container
          image: monsterdigital.azurecr.io/mdp-webapp-shipping:prod
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          readinessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 15
            timeoutSeconds: 2
            failureThreshold: 5
