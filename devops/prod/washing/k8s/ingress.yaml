apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: webapp-washing-ingress
  namespace: mdp
  annotations:
    kubernetes.io/ingress.class: azure/application-gateway
spec:
  rules:
    - host: mdpprod-m.monsterdigital.biz
      http:
        paths:
          - path: /washing
            pathType: Prefix
            backend:
              service:
                name: webapp-washing
                port:
                  number: 80
